---

- name : installation de mediawiki
  become: true
  hosts: mediawiki
  tasks:

    - name: install par script
      command: "php install.php --dbname wikiFV --dbuser userwiki --dbpass  --dbserver localhost --server 'http://192.168.1.26' --dbtype mysql --wiki wikiFV --pass azerty123admin --lang fr --scriptpath /wikidevops --confpath /home/appli/MWA_mediawiki/LIVR/wikidevops wikiFV admin"
      args:
        chdir: /home/appli/MWA_mediawiki/LIVR/wikidevops/maintenance


...