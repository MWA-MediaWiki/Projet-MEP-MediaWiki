---

- name : installation de mediawiki
  become: true
  hosts: mediawiki
  tasks:

    #Création bdd et user
    - name: Create MySQL DB
      mysql_db:
        name: wikiFV
        state: present

    - name: Create MySQL user
      mysql_user:
        name: userwiki
        host: localhost
        password: az
        priv: wikiFV.*:ALL

...